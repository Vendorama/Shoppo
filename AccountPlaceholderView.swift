import SwiftUI

struct AccountPlaceholderView: View {
    @State private var showAccountSheet = false

    var body: some View {
        ScrollView {
            VStack(alignment: .leading) {
                
                Text("account details and forms")
                
            }
        }
    }
}
