import SwiftUI

struct LoginViewPlaceholder: View {
    @State private var showLoginSheet = false

    var body: some View {
        ScrollView {
            VStack(alignment: .leading) {
                
                Text("login form")
                
            }
        }
    }
}
